{
  "name": "uneve-app",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "linter": "yarn workspace @uneve/eslint-config"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "@uneve/eslint-config": "*"
  }
}
